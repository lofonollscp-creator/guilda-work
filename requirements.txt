flask>=3.0
flask-limiter>=4.0
waitress>=3.0
icalendar>=5.0
keyring>=24.0
keyrings.cryptfile>=1.3.9
pyinstaller>=6.0
pywebview>=5.0
pystray>=0.19
keyboard>=0.13
Pillow>=10.0
# Export PDF del registro de fichaje (app/fichaje_export.py) -- puro Python
# instalable por pip, sin binario de sistema externo (a diferencia de
# weasyprint/wkhtmltopdf, que necesitan Pango/Cairo o un binario aparte).
reportlab>=4.0
# Notificaciones push a la app móvil (app/push.py) -- única excepción al
# criterio de "solo urllib" del resto de integraciones de app/*.py: la API
# HTTP v1 de FCM exige un JWT firmado RS256 canjeado por un token OAuth2,
# y reimplementar eso a mano no compensa frente a la librería oficial.
google-auth>=2.35
requests>=2.31
# Captcha de prueba de trabajo para el login (app/captcha.py) -- ALTCHA,
# self-hosted, sin servicio de terceros ni CDN.
altcha>=2.1
