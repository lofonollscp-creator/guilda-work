name: guilda_work_mobile
description: "Guilda Work: registro de actividad, tareas, correo, fichaje y asistente IA."
# The following line prevents the package from being accidentally published to
# pub.dev using `flutter pub publish`. This is preferred for private packages.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

# The following defines the version and build number for your application.
# A version number is three numbers separated by dots, like 1.2.43
# followed by an optional build number separated by a +.
# Both the version and the builder number may be overridden in flutter
# build by specifying --build-name and --build-number, respectively.
# In Android, build-name is used as versionName while build-number used as versionCode.
# Read more about Android versioning at https://developer.android.com/studio/publish/versioning
# In iOS, build-name is used as CFBundleShortVersionString while build-number is used as CFBundleVersion.
# Read more about iOS versioning at
# https://developer.apple.com/library/archive/documentation/General/Reference/InfoPlistKeyReference/Articles/CoreFoundationKeys.html
# In Windows, build-name is used as the major, minor, and patch parts
# of the product and file versions while build-number is used as the build suffix.
version: 1.0.0+1

environment:
  sdk: ^3.12.2

# Dependencies specify other packages that your package needs in order to work.
# To automatically upgrade your package dependencies to the latest versions
# consider running `flutter pub upgrade --major-versions`. Alternatively,
# dependencies can be manually updated by changing the version numbers below to
# the latest version available on pub.dev. To see which dependencies have newer
# versions available, run `flutter pub outdated`.
dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter
  intl: ^0.20.2

  # The following adds the Cupertino Icons font to your application.
  # Use with the CupertinoIcons class for iOS style icons.
  cupertino_icons: ^1.0.8
  dio: ^5.10.0
  flutter_secure_storage: ^10.3.1
  shared_preferences: ^2.5.5
  flutter_widget_from_html: ^0.17.2
  file_picker: ^11.0.0
  path_provider: ^2.1.6
  open_filex: ^4.7.0
  webview_flutter: ^4.14.1
  matrix: ^8.1.0
  app_links: ^7.0.0
  url_launcher: ^6.3.2
  sqflite: ^2.4.1
  path: ^1.9.0
  uuid: ^4.5.1
  connectivity_plus: ^6.1.3
  # Logos reales de las herramientas (SVG servidos por el propio backend
  # desde app/static/logos/, ver herramientas_screen.dart) -- Image.network
  # de Flutter no sabe pintar SVG.
  flutter_svg: ^2.2.1
  # Asistente de IA con voz (Fase V3 del plan "eventual-herding-kitten"):
  # dictado (STT) y lectura en voz alta de las respuestas (TTS), ver
  # ia_chat_screen.dart. Necesitan permiso de micrófono -- ver
  # NSMicrophoneUsageDescription en ios/Runner/Info.plist y RECORD_AUDIO en
  # android/app/src/main/AndroidManifest.xml.
  speech_to_text: ^7.0.0
  flutter_tts: ^4.2.0
  firebase_core: ^3.10.0
  firebase_messaging: ^15.2.0
  flutter_local_notifications: ^18.0.1
  # Calendario fiscal (Fase F5 de la ampliación "eventual-herding-kitten"):
  # primera vista de calendario mensual en móvil (no existía ninguna, ni
  # de tareas ni fiscal) -- paquete consolidado de pub.dev en vez de
  # construir una rejilla de mes a mano, mismo criterio pragmático que al
  # añadir speech_to_text/flutter_tts cuando hizo falta una capacidad
  # nueva. Ver tareas_calendario_screen.dart.
  table_calendar: ^3.1.2
  # Espacio de ajustes de usuario (Fase G1): subir avatar desde galería o
  # cámara en perfil_screen.dart.
  image_picker: ^1.1.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  mocktail: ^1.0.4
  # Backend FFI de sqflite para poder ejecutar tests de verdad contra SQLite
  # sin un dispositivo/emulador real (sqflite normal habla por platform
  # channel, que no existe en el entorno de `flutter test`).
  sqflite_common_ffi: ^2.3.4+4

  # The "flutter_lints" package below contains a set of recommended lints to
  # encourage good coding practices. The lint set provided by the package is
  # activated in the `analysis_options.yaml` file located at the root of your
  # package. See that file for information about deactivating specific lint
  # rules and activating additional ones.
  flutter_lints: ^6.0.0

  # Genera los sets de iconos de iOS/Android a partir de assets/icon/*.png
  # (ver flutter_launcher_icons.yaml) -- Fase 4 del plan
  # "eventual-herding-kitten". Solo hace falta en tiempo de generación, no en
  # runtime, por eso vive en dev_dependencies.
  flutter_launcher_icons: ^0.14.3

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter packages.
flutter:
  # Genera lib/l10n/app_localizations.dart a partir de los .arb (ver
  # l10n.yaml) en cada `flutter pub get`/build -- no se versiona el
  # fichero generado.
  generate: true

  # The following line ensures that the Material Icons font is
  # included with your application, so that you can use the icons in
  # the material Icons class.
  uses-material-design: true

  # Logo de marca (calcado de app/static/logo.png) para el encabezado de
  # login/registro -- ver assets/branding/logo.png (Fase 5 del plan
  # "eventual-herding-kitten"). assets/icon/ NO se declara aquí a propósito:
  # solo lo usa flutter_launcher_icons en tiempo de generación, no en runtime.
  assets:
    - assets/branding/logo.png

  # An image asset can refer to one or more resolution-specific "variants", see
  # https://flutter.dev/to/resolution-aware-images

  # For details regarding adding assets from package dependencies, see
  # https://flutter.dev/to/asset-from-package

  # To add custom fonts to your application, add a fonts section here,
  # in this "flutter" section. Each entry in this list should have a
  # "family" key with the font family name, and a "fonts" key with a
  # list giving the asset and other descriptors for the font. For
  # example:
  # fonts:
  #   - family: Schyler
  #     fonts:
  #       - asset: fonts/Schyler-Regular.ttf
  #       - asset: fonts/Schyler-Italic.ttf
  #         style: italic
  #   - family: Trajan Pro
  #     fonts:
  #       - asset: fonts/TrajanPro.ttf
  #       - asset: fonts/TrajanPro_Bold.ttf
  #         weight: 700
  #
  # For details regarding fonts from package dependencies,
  # see https://flutter.dev/to/font-from-package
