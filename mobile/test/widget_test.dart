// Placeholder: los tests de widgets de verdad (con mocks del ApiClient y de
// SessionService) llegan en las sub-fases siguientes de la Fase 4, cuando
// haya pantallas con lógica propia que valga la pena probar de forma
// aislada. Por ahora, la verificación de esta sub-fase es funcional: correr
// la app en el emulador Android y hacer login/registro reales contra
// serve.py (ver plan de la app móvil).
import 'package:flutter_test/flutter_test.dart';

void main() {
  test('placeholder', () {
    expect(1 + 1, 2);
  });
}
